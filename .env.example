# Beyond the Chatbot - Tour CLI configuration
# Copy to `.env` and fill in if you need to override defaults.

# RNG seed used across demos when they land.
# DEMO_SEED=42

# Where the CLI caches anything heavy. Default: ~/.cache/beyond-the-chatbot
# CACHE_DIR=~/.cache/beyond-the-chatbot
